This repository demonstrates what happens to the commit graph when many trivial
merges are performed.

The resuling ugly history graph contains "merge bubbles". Compare with
[using-rebase](../../../using-rebase).
